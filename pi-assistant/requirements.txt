# ─────────────────────────────────────────────────────────────────────────────
# Pi Assistant — Python dependencies
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Web dashboard & API server
flask==3.0.3
flask-login==0.6.3
flask-wtf==1.2.1          # CSRF protection on forms
waitress==3.0.0            # Production WSGI server (pure Python, Pi-compatible)

# Configuration & environment
pyyaml==6.0.2
python-dotenv==1.0.1

# Scheduling
apscheduler==3.10.4

# HTTP requests (AI APIs, external services)
httpx==0.27.0              # Async-capable, replaces requests
tenacity==8.4.1            # Retry logic with exponential back-off

# Security
cryptography==42.0.8       # Token signing, secret management
bcrypt==4.1.3              # Password hashing for dashboard auth

# Database (long-term memory — SQLite wrapper)
SQLAlchemy==2.0.31         # ORM; SQLite driver is built into Python
alembic==1.13.2            # Schema migrations for future upgrades

# Utilities
rich==13.7.1               # Beautiful console output and log formatting
click==8.1.7               # CLI helper for management commands
python-dateutil==2.9.0     # Date/time parsing utilities

# Testing
pytest==8.3.2
pytest-mock==3.14.0
